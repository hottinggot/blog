let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_333‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_333‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_333‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_333‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_333‍.w("./api",[["default",["api"],function(v){api=v}]]);_333‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

//서버와 데이터베이스 연결
mongoose
  .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => _333‍.g.console.error(e));

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(jwtMiddleware);

app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(4000, () => {
  _333‍.g.console.log('Listening to port %d', port);
});
